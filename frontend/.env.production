# NOTE: Do NOT set VITE_API_URL to an HTTP endpoint when the frontend is HTTPS.
# The Vercel proxy (vercel.json rewrites) handles /api/* calls correctly.
# Only set VITE_API_URL if you have HTTPS on your EC2 (e.g. with nginx + SSL cert).
# VITE_API_URL=https://your-domain.com/api
